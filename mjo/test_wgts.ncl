; These files are loaded by default in NCL V6.2.0 and newer
; load "$NCARG_ROOT/lib/ncarg/nclscripts/csm/gsn_code.ncl"   
; load "$NCARG_ROOT/lib/ncarg/nclscripts/csm/gsn_csm.ncl"    
; load "$NCARG_ROOT/lib/ncarg/nclscripts/csm/contributed.ncl"

; File with mjo_function loaded manually
load "$NCARG_ROOT/lib/ncarg/nclscripts/csm/diagnostics_cam.ncl"

; ------------- Read in data -----------------------

WORKDIR = systemfunc("pwd")
diri = "/data/anomaly/"

filename = "QBOi.EXP1.AMIP.001.flut.day.anom.nc" ; generated by MJO workflow 2_MJO_daily_anom
vName = "FLUT" ; name of variable in the file

f = addfile(WORKDIR + diri + filename, "r")

X = f->$vName$(:, {-30:30},:)
wgt = filwgts_lanczos(201, 2, 1./100., 1./20., 1.0)
;printVarSummary(X)

;print(X(0:5, 0, 0))

c = wgt_runave_Wrap(X(lat|:, lon|:, time|:), wgt, 0)
printVarSummary(c)
print(c(0:5, 0, 0))


;x = dim_rmvmean(x)
;printVarSummary(x)

