;******************************************************
; Example script for working with "mjo_spectra_season"
; ncl mjo_spectra_season.ncl >> mjo_output/mjo_spectra_season_winter.txt
; ncl mjo_spectra_season.ncl >> mjo_output/mjo_spectra_season_summer.txt
; ncl mjo_spectra_season.ncl >> mjo_output/mjo_spectra_season_annual.txt
;******************************************************
; These files are loaded by default in NCL V6.2.0 and newer
; load "$NCARG_ROOT/lib/ncarg/nclscripts/csm/gsn_code.ncl"   
; load "$NCARG_ROOT/lib/ncarg/nclscripts/csm/gsn_csm.ncl"    
; load "$NCARG_ROOT/lib/ncarg/nclscripts/csm/contributed.ncl"

; File with mjo_function loaded manually
load "$NCARG_ROOT/lib/ncarg/nclscripts/csm/diagnostics_cam.ncl"

; ------------- Read in data -----------------------

WORKDIR = systemfunc("pwd")
diri = "/data/anomaly/"

filename = "QBOi.EXP1.AMIP.001.flut.day.anom.nc" ; generated by MJO workflow 2_MJO_daily_anom
vName = "FLUT" ; name of variable in the file

f = addfile(WORKDIR + diri + filename, "r")

; ------------- Slice Input -----------------------

twStrt = 19790101 ; time window start
twLast = 19811231 ; time window end

date = cd_calendar(f->time, -2) ; YYYYMMDD
iStrt = ind(date.eq.twStrt)
iLast = ind(date.eq.twLast)
delete(date)

X = f->$vName$(iStrt:iLast,:,:)

time = X&time
date = cd_calendar(time, -2) ; YYYYMMDD

; ------------- Input with MJO Cross Parameters -----------------------

lat = f->lat
wgty = cos(0.01745329*lat) ; MJO Clivar specify cos(pi/180 * lat)
copy_VarCoords(lat, wgty)

; ------------- Calculate MJO Spectra Season -----------------------

seasonName = "winter" ; "winter", "summer", "annual"

smjo = mjo_spectra_season(X, date, wgty, seasonName, False)
;print(smjo)

print("Season Name: " + seasonName)
print("Bandwidth (bw): " + smjo@bw)
print("Average Variance (vari/xvari): " + smjo@xvari)
print("Average Lag One Day Autocorrelation (xlag1): " + smjo@xlag1)
print("Segment Averaged Spectra (spcx):")
print(smjo@spcx)
print("Frequencies (frq):")
print(smjo@frq)
